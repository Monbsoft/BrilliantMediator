name: Publish NuGet Package

on:
  push:
    tags:
      - 'v*.*.*'  # Publiera lors du push d'un tag version

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore BrilliantMediator.slnx

    - name: Build project
      run: dotnet build BrilliantMediator.slnx --configuration Release --no-restore

    - name: Pack NuGet package
      run: dotnet pack BrilliantMediator.slnx --configuration Release --no-build -o ./output

    - name: Push package to NuGet
      run: dotnet nuget push "./output/*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

    
